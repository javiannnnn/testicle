import { AdminNav } from "@/components/AdminNav";
import { events } from "@/lib/events";

export default function RegistrationsPage() { return <><AdminNav /><main className="mx-auto w-full max-w-7xl px-5 py-10"><h1 className="font-display text-3xl font-bold text-slate-950">Registrations</h1><p className="mt-2 text-slate-600">A frontend-only overview; updates will connect to the backend later.</p><div className="mt-7 grid gap-4 md:grid-cols-2">{events.map((event) => <section key={event.id} className="rounded-2xl border border-slate-200 bg-white p-6"><h2 className="font-display text-xl font-bold text-slate-950">{event.title}</h2><dl className="mt-5 grid grid-cols-2 gap-4 text-sm"><div><dt className="text-slate-500">Registered</dt><dd className="mt-1 text-xl font-semibold text-slate-950">{event.registered}</dd></div><div><dt className="text-slate-500">Waitlist</dt><dd className="mt-1 text-xl font-semibold text-slate-950">{event.waitlisted}</dd></div></dl></section>)}</div></main></>; }
