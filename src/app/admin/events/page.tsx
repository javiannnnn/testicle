import { AdminNav } from "@/components/AdminNav";
import { events, spotsLeft } from "@/lib/events";
import { formatEventDateTime } from "@/lib/event-view";

export default function AdminEventsPage() { return <><AdminNav /><main className="mx-auto w-full max-w-7xl px-5 py-10"><h1 className="font-display text-3xl font-bold text-slate-950">Event operations</h1><p className="mt-2 text-slate-600">Frontend preview of capacity and publishing decisions.</p><div className="mt-7 overflow-x-auto rounded-2xl border border-slate-200 bg-white"><table className="min-w-full text-left text-sm"><thead className="border-b border-slate-200 bg-slate-50 text-slate-600"><tr><th className="px-5 py-3 font-semibold">Event</th><th className="px-5 py-3 font-semibold">When</th><th className="px-5 py-3 font-semibold">Capacity</th><th className="px-5 py-3 font-semibold">Availability</th></tr></thead><tbody>{events.map((event) => <tr key={event.id} className="border-b border-slate-100 last:border-0"><td className="px-5 py-4 font-semibold text-slate-950">{event.title}<p className="mt-1 font-normal text-slate-500">{event.location}</p></td><td className="px-5 py-4 text-slate-700">{formatEventDateTime(event.dateISO)}</td><td className="px-5 py-4 text-slate-700">{event.registered}/{event.capacity}</td><td className="px-5 py-4 text-slate-700">{event.status === "waitlist" ? `${event.waitlisted} on waitlist` : event.status === "closed" ? "Closed" : `${spotsLeft(event)} places left`}</td></tr>)}</tbody></table></div></main></>; }
